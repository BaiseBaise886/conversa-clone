# WhatsApp Bridge Service Configuration

# Port for the bridge service
PORT=3000

# Laravel API URL (for sending webhooks)
LARAVEL_API_URL=http://localhost:8000

# API Key for authentication (must match WHATSAPP_API_KEY in Laravel .env)
API_KEY=your_secure_api_key_here
